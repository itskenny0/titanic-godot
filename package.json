{
  "name": "retanic-godot",
  "private": true,
  "type": "module",
  "scripts": {
    "build:engine": "node tools/bundle-engine.mjs && python3 tools/prepare-notices.py",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
